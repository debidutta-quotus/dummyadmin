import './SplashScreen.css';

const SplashScreen = () => {
  return (
    <div id="splash-screen">
      <div id="title">Restominder</div>
      <div id="subtitle">Connect the World with Food</div>
    </div>
  );
};

export default SplashScreen;
